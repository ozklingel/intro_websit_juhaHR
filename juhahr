import { Card, CardContent } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { motion } from "framer-motion";

export default function LandingPage() {
  return (
    <div className="min-h-screen bg-white text-gray-900">
      {/* Hero */}
      <section className="text-center py-20 px-6 bg-gradient-to-b from-gray-50 to-white">
        <motion.h1
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.6 }}
          className="text-4xl md:text-6xl font-bold mb-6"
        >
          מערכת לניהול כח אדם פלוגתי
        </motion.h1>

        <p className="text-xl max-w-2xl mx-auto mb-8 leading-relaxed">
          אפליקציה לזכרו של רס״ן יוחאי (ג׳וחא) קלנגל ז״ל – שמעמידה את הלוחם במרכז
          ומאפשרת ניהול פלוגה חכם, מקצועי ונגיש יותר בשטח.
        </p>

        <div className="flex gap-4 justify-center">
          <Button onClick={() => window.open("https://juhahr.com", "_blank")}>לאתר המערכת</Button>
          <Button variant="outline" onClick={() => document.getElementById('video')?.scrollIntoView({ behavior: 'smooth' })}>
            סרטון הדרכה
          </Button>
        </div>
      </section>

      {/* About */}
      <section className="py-16 px-6 max-w-4xl mx-auto">
        <Card>
          <CardContent className="p-6 leading-relaxed text-right">
            <p className="mb-4">
              בשעה טובה אנו משיקים אפליקציה לניהול כח אדם פלוגתי לזכרו של "ג׳וחא".
            </p>

            <p className="mb-4">
              רס״ן יוחאי (ג׳וחא) קלנגל ז״ל היה מ״פ בגדוד צבר בגבעתי, אשר נפל בפעילות מבצעית
              במורדות הר דב ביום ח׳ בשבט תשע״ה (28.1.2015), והוא בן 25 בנופלו.
            </p>

            <p className="mb-4">
              ג׳וחא היה מפקד של אנשים לפני הכל. מתוך הערכים הללו פותחה המערכת – כדי לתת
              למפקדים כלים טובים יותר לניהול כוח האדם בפלוגה.
            </p>

            <p>
              הגרסה הנוכחית מותאמת לפלוגת מילואים, עם אפשרות התאמה לסדיר.
            </p>
          </CardContent>
        </Card>
      </section>

      {/* CTA */}
      <section className="text-center py-12 bg-gray-100">
        <h2 className="text-2xl font-bold mb-4">מעוניינים להצטרף?</h2>
        <p className="mb-6">צרו קשר עם עוז קלינגל להצטרפות והטמעה ביחידה שלכם</p>
        <Button onClick={() => window.location.href = "tel:0549247616"}>
          054-9247616
        </Button>
      </section>

      {/* Video */}
      <section id="video" className="py-16 px-6 max-w-4xl mx-auto text-center">
        <h2 className="text-2xl font-bold mb-6">סרטון הדרכה</h2>

        <div className="aspect-video">
          <iframe
            className="w-full h-full rounded-xl"
            src="https://www.youtube.com/embed/kQiwviYiMgc"
            title="הדרכה"
            allowFullScreen
          />
        </div>
      </section>

      {/* Footer */}
      <footer className="text-center py-8 text-sm text-gray-500">
        © {new Date().getFullYear()} מערכת JuharHR
      </footer>
    </div>
  );
}
